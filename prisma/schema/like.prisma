model Like {
    id         Int      @id @default(autoincrement())
    ip_address String
    created_at DateTime @default(now())

    // Relation (many Likes → 1 Blog)
    blogId Int
    blog   Blog @relation(fields: [blogId], references: [id])
}
